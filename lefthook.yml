pre-commit:
  parallel: true
  jobs:
    - name: gofmt
      root: backend
      glob: "**/*.go"
      run: gofmt -w {staged_files}
      stage_fixed: true
    - name: eslint
      root: frontend
      glob: "**/*.{ts,tsx,js,jsx}"
      run: npx eslint --fix {staged_files}
      stage_fixed: true
